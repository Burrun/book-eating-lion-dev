apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: ${K8S_NAMESPACE}
  labels:
    app: order-service
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
