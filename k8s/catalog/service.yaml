apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: lion-app
  labels:
    app: catalog-service
spec:
  type: ClusterIP
  selector:
    app: catalog-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
