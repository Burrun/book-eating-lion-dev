# 로컬 dev 서버(`npm run dev`)가 읽는 값. 개인 설정은 .env / .env.local 에 두고,
# 이 파일에는 팀 전체가 같은 값을 써야 하는 것만 커밋한다.

# 한 번에 긁을 수 있는 원문 길이(자). .env.production 과 같은 값으로 둔다 — 다르면
# 로컬에서만 통과하고 배포 후 저장이 거절되는 차이가 생긴다.
VITE_HIGHLIGHT_MAX_SELECTED_CHARS=1000
